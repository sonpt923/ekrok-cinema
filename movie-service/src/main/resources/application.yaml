spring:
  application:
    name: movie-service
  datasource:
    url: jdbc:mysql://localhost:3306/movie?useSSL=true
    username: root
    password: Son191203
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false