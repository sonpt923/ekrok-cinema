spring:
  application:
    name: user-service
  config:
    import: "optional:configserver:http://localhost:8088"
  datasource:
    url: jdbc:mysql://localhost:3306/keycloak?useSSL=true
    username: root
    password: Son191203
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  cache:
    type: redis
    redis:
      cache-null-values: false
      use-key-prefix: true
      time-to-live: 3000
  data:
    redis:
      host: redis-14618.c292.ap-southeast-1-1.ec2.redns.redis-cloud.com
      port: 14618
      password: bq2UkSMCAxrdm1aYvJnpkXGokVZNqapK
      havePassword: YES
      username: default
keycloak:
  url_base: http://localhost:9090/realms/master/protocol/openid-connect/token

